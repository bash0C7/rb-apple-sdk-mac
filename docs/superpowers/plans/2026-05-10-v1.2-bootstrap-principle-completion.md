# v1.2 Bootstrap Principle Completion Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v1.2 spec § 4 Phase 4a 完了条件「`symbols.swift_imported_name` カラム populated」を非自明な件数で満たし、 `rake test:release_quality` を green にして、 `feature/v1.2-bootstrap-principle` を main へ merge 可能状態にする。

**Architecture:** 2 件の latent defect (SwiftOverlay の `EXTENSION_HEADER_RE` が実 Apple `.swiftinterface` の `extension Module.Type [: Conformance] [where ...] {` を一切 match せん regex / `Store#vec_insert` が vec0 virtual table の `INSERT OR REPLACE` 非対応で `SQLite3::SQLException` を上げる) を TDD で修正 → KB 全 rebuild (embedder ON) → populated count を test-unit assert で verify → release_quality 走らせる → push して main merge は user に委譲 (memory rule: main 直 push は人間に渡す)。

**Tech Stack:** Ruby 4.x master + `RUBY_BOX=1` namespace isolation, test-unit, SQLite3 + sqlite-vec, Swift 6.3 + clang AST, regex-based `.swiftinterface` parser, `screen -dmS` detached long-batch (CLAUDE.md `~/dev/src` ロングバッチ pattern)。

---

## File Structure

| File | Role |
|---|---|
| `knowledge/lib/rb_apple_sdk_knowledge/importer/swift_overlay.rb` | Modify: `EXTENSION_HEADER_RE` regex + `extract_extension_blocks` 末尾 segment 抽出 |
| `knowledge/lib/rb_apple_sdk_knowledge/store.rb` | Modify: `vec_insert` を DELETE + INSERT pattern に (vec0 idempotence) |
| `knowledge/lib/rb_apple_sdk_knowledge/importer.rb` | Modify (T4 のみ): `Pipeline#insert_one` の rescue を broaden (defensive depth) |
| `knowledge/test/importer/swift_overlay_extension_scan_test.rb` | Modify: 実 Apple 形式 (`Foundation.URL`, conformance, where, nested) に対する RED test 追加 |
| `knowledge/test/fixtures/swift_overlay/foundation_real.swiftinterface` | Create: 実 Apple `.swiftinterface` から抜粋した RED fixture |
| `knowledge/test/test_store.rb` | Modify: `vec_insert` 同 symbol_id 二度書きの idempotence test |
| `knowledge/test/integration/full_rebuild_assertions_test.rb` | Create: rebuild 完了後の populated count 等を test-unit assert で固定 |
| `test/integration/examples_v12_e2e_test.rb` | Create (T11): `avspeech_synth.rb` / `vision_ocr.rb` / `discover_escape.rb` を 1 example = 1 test method で `RUBY_BOX=1 ruby ...` を `assert_match` で検証 |
| `tmp/longrun/kb-rebuild-v12-final.log` | Create (T8): screen detached log target |

`tooling/` / `irb/` / `ext/` は触らん。 examples は既存ファイルを変えない (e2e wrapper のみ追加)。

---

## Task 1: SwiftOverlay 実 Apple `.swiftinterface` 形式の RED test

**Files:**
- Create: `knowledge/test/fixtures/swift_overlay/foundation_real.swiftinterface`
- Modify: `knowledge/test/importer/swift_overlay_extension_scan_test.rb`

- [ ] **Step 1: 実 Apple Foundation `.swiftinterface` から fixture 抜粋作成**

Create `knowledge/test/fixtures/swift_overlay/foundation_real.swiftinterface`:

```
extension Foundation.URL {
  public init?(string: Swift.String)
  public var absoluteString: Swift.String { get }
}
extension Foundation.TermOfAddress : Swift.Codable {
  public func encode(to encoder: any Swift.Encoder) throws
}
extension Foundation._KeyValueCodingAndObservingPublishing where Self : ObjectiveC.NSObject {
  public func publisher<Value>(for keyPath: Swift.KeyPath<Self, Value>) -> ObjectiveC.NSObject.KeyValueObservingPublisher<Self, Value>
}
extension Foundation.NSKeyValueObservedChange : Swift.Sendable where Value : Swift.Sendable {
}
extension Foundation.URL.ParseStrategy {
  public func parse(_ value: Swift.String) throws -> Foundation.URL
}
```

(意図: 1) module-qualified simple、 2) module-qualified + conformance、 3) where 句、 4) conformance + where 同時、 5) nested type 3 segment — 全 form を 1 fixture で網羅)

- [ ] **Step 2: extension_scan_test.rb に RED test 追加**

Modify `knowledge/test/importer/swift_overlay_extension_scan_test.rb`. Append before final `end`:

```ruby
  def test_extracts_module_qualified_simple
    source = "extension Foundation.URL {\n  public init?(string: Swift.String)\n}\n"
    blocks = @overlay.send(:extract_extension_blocks, source)
    assert_equal 1, blocks.length
    assert_equal "URL", blocks[0][:klass]
  end

  def test_extracts_module_qualified_with_conformance
    source = "extension Foundation.TermOfAddress : Swift.Codable {\n  public func encode(to encoder: any Swift.Encoder) throws\n}\n"
    blocks = @overlay.send(:extract_extension_blocks, source)
    assert_equal 1, blocks.length
    assert_equal "TermOfAddress", blocks[0][:klass]
  end

  def test_extracts_module_qualified_with_where_clause
    source = "extension Foundation._KeyValueCodingAndObservingPublishing where Self : ObjectiveC.NSObject {\n  public func publisher<Value>(for keyPath: Swift.KeyPath<Self, Value>) -> Swift.Int\n}\n"
    blocks = @overlay.send(:extract_extension_blocks, source)
    assert_equal 1, blocks.length
    assert_equal "_KeyValueCodingAndObservingPublishing", blocks[0][:klass]
  end

  def test_extracts_module_qualified_with_conformance_and_where
    source = "extension Foundation.NSKeyValueObservedChange : Swift.Sendable where Value : Swift.Sendable {\n}\n"
    blocks = @overlay.send(:extract_extension_blocks, source)
    assert_equal 1, blocks.length
    assert_equal "NSKeyValueObservedChange", blocks[0][:klass]
  end

  def test_extracts_nested_type_3_segments
    source = "extension Foundation.URL.ParseStrategy {\n  public func parse(_ value: Swift.String) throws -> Foundation.URL\n}\n"
    blocks = @overlay.send(:extract_extension_blocks, source)
    assert_equal 1, blocks.length
    assert_equal "ParseStrategy", blocks[0][:klass]
  end

  def test_extracts_all_forms_from_real_fixture
    fixture = File.expand_path("../fixtures/swift_overlay/foundation_real.swiftinterface", __dir__)
    source  = File.read(fixture)
    blocks  = @overlay.send(:extract_extension_blocks, source)
    klasses = blocks.map { |b| b[:klass] }
    # 5 different extension forms in the fixture, all must extract
    assert_equal 5, blocks.length, "expected 5 extension blocks from real fixture, got #{blocks.length}: #{klasses.inspect}"
    assert_includes klasses, "URL"
    assert_includes klasses, "TermOfAddress"
    assert_includes klasses, "_KeyValueCodingAndObservingPublishing"
    assert_includes klasses, "NSKeyValueObservedChange"
    assert_includes klasses, "ParseStrategy"
  end
```

- [ ] **Step 3: RED 確認**

Run:
```bash
bundle exec ruby -Iknowledge/lib -Iknowledge/test knowledge/test/importer/swift_overlay_extension_scan_test.rb
```
Expected: 6 new assertions FAIL because current `EXTENSION_HEADER_RE = /\bextension\s+(\w+)\s*\{/` matches 0/5 of these forms (`\w+` doesn't span dots).

- [ ] **Step 4: RED commit**

```bash
git add knowledge/test/fixtures/swift_overlay/foundation_real.swiftinterface knowledge/test/importer/swift_overlay_extension_scan_test.rb
git commit -m "test(knowledge): RED — SwiftOverlay must extract module-qualified extension forms

Real Apple .swiftinterface uses 'extension Module.Type [: Conformance]
[where ...] {' for all 1320 extensions in Foundation. Current
EXTENSION_HEADER_RE = /\\\\bextension\\\\s+(\\\\w+)\\\\s*\\\\{/ matches 0 of them.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: SwiftOverlay GREEN — regex 拡張 + 末尾 segment 抽出

**Files:**
- Modify: `knowledge/lib/rb_apple_sdk_knowledge/importer/swift_overlay.rb:16` (EXTENSION_HEADER_RE) and `:39-66` (extract_extension_blocks)

- [ ] **Step 1: regex を dotted name + 任意 conformance / where に拡張**

Replace line 16 in `knowledge/lib/rb_apple_sdk_knowledge/importer/swift_overlay.rb`:

```ruby
      # Matches: extension <DottedName>(.\w+)*(\s*:\s*<Conformances>)?(\s+where\s+...)?\s*\{
      # Captures: [1] dotted name (e.g. "Foundation.URL", "Foundation.URL.ParseStrategy")
      # Real Apple .swiftinterface always uses module-qualified names; klass key is
      # taken as the LAST dotted segment by extract_extension_blocks.
      EXTENSION_HEADER_RE = /\bextension\s+([A-Za-z_][\w.]*)[^{]*\{/.freeze
```

- [ ] **Step 2: extract_extension_blocks の末尾 segment 抽出を更新**

Replace lines 43-46 in same file (the `if (m = scanner.scan_until(EXTENSION_HEADER_RE))` block's klass capture):

```ruby
          if (m = scanner.scan_until(EXTENSION_HEADER_RE))
            dotted     = scanner.captures.first || m[/extension\s+([A-Za-z_][\w.]*)/, 1]
            # Real Apple .swiftinterface uses module-qualified or nested-type
            # names: "Foundation.URL", "Foundation.URL.ParseStrategy". The KB
            # key is the type identifier alone — last dotted segment.
            klass      = dotted.split(".").last
            body_start = scanner.pos
            depth      = 1
```

- [ ] **Step 3: GREEN 確認**

```bash
bundle exec ruby -Iknowledge/lib -Iknowledge/test knowledge/test/importer/swift_overlay_extension_scan_test.rb
```
Expected: ALL pass (existing 5 + new 6 = 11 tests green). Existing `assert_equal "AVCaptureDevice"` tests still pass because `\w+` is a subset of `[A-Za-z_][\w.]*`.

- [ ] **Step 4: 全 swift_overlay test suite 走らせて回帰なし確認**

```bash
for f in knowledge/test/importer/swift_overlay_*.rb; do
  bundle exec ruby -Iknowledge/lib -Iknowledge/test "$f"
done
```
Expected: 全 file PASS (decl_parse / selector_recon / pipeline / scan / overlay 5 file)

- [ ] **Step 5: GREEN commit**

```bash
git add knowledge/lib/rb_apple_sdk_knowledge/importer/swift_overlay.rb
git commit -m "fix(knowledge): GREEN — SwiftOverlay regex matches real Apple .swiftinterface

EXTENSION_HEADER_RE 拡張: 1) module-qualified dotted name (Foundation.URL),
2) conformance (: Swift.Codable), 3) where clause, 4) nested type 3 segments
(Foundation.URL.ParseStrategy). klass key は最終 dotted segment。

Why fix: prior regex matched 0/1320 Foundation extensions in real
CommandLineTools SDK input → swift_imported_name 列が rebuild 後 0 件で
spec § 4 Phase 4a 完了条件を満たさんかった。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: Pipeline integration RED — 実 fixture で swift_imported_name persist 確認

**Files:**
- Modify: `knowledge/test/importer/swift_overlay_pipeline_test.rb`

- [ ] **Step 1: 既存 pipeline_test.rb の現状把握**

```bash
sed -n '1,60p' knowledge/test/importer/swift_overlay_pipeline_test.rb
```
(scope check: 既存 test が hand-crafted fixture path を参照しとるか確認)

- [ ] **Step 2: 実 fixture を import! して swift_imported_name が複数件 populate されるか assert**

Append to `knowledge/test/importer/swift_overlay_pipeline_test.rb` before final `end`:

```ruby
  def test_real_apple_fixture_populates_swift_imported_name
    Dir.mktmpdir do |dir|
      store   = AppleSDKKnowledge::Store.open(File.join(dir, "test.sqlite"))
      fixture = File.expand_path("../fixtures/swift_overlay/foundation_real.swiftinterface", __dir__)
      AppleSDKKnowledge::Importer::SwiftOverlay.new(store).import!(framework: "Foundation", path: fixture)

      count = store.db.execute(
        "SELECT COUNT(*) FROM symbols WHERE swift_imported_name IS NOT NULL AND swift_imported_name != ''"
      ).first.first
      assert_operator count, :>=, 4,
        "expected >=4 rows with swift_imported_name from 5 extension blocks (URL has 1 init + 1 var, others 1 each), got #{count}"

      # Spot-check: URL.init?(string:) → ObjC selector "initWithString:" with swift_imported_name "init(string:)"
      row = store.db.execute(<<~SQL, ["Foundation", "URL", "initWithString:"]).first
        SELECT s.swift_imported_name FROM symbols s
        JOIN symbols p ON s.parent_id = p.id
        JOIN frameworks f ON s.framework_id = f.id
        WHERE f.name = ? AND p.name = ? AND s.name = ?
      SQL
      refute_nil row, "URL.initWithString: row missing"
      assert_equal "init(string:)", row[0]

      store.close
    end
  end
```

- [ ] **Step 3: RED 確認 (Task 2 GREEN 済なので、ここで RED → 別 root cause 発覚なら追加修正、 GREEN ならそのまま)**

```bash
bundle exec ruby -Iknowledge/lib -Iknowledge/test knowledge/test/importer/swift_overlay_pipeline_test.rb
```
Expected: PASS (Task 2 で regex 直したので、 fixture 全 form が認識され swift_imported_name 入る)。
もし FAIL なら parse_decl_line / kind_string_for / upsert_decl の secondary defect — その時は systematic-debugging 再起動 (Phase 4 の 3-fix 規律遵守、 4 fix 目に行かんと user 報告)。

- [ ] **Step 4: commit**

```bash
git add knowledge/test/importer/swift_overlay_pipeline_test.rb
git commit -m "test(knowledge): integration — real Apple fixture populates swift_imported_name

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: Store#vec_insert idempotence RED

**Files:**
- Modify: `knowledge/test/test_store.rb`

- [ ] **Step 1: 既存 test_store.rb で vec_insert path をテストしとるか確認**

```bash
grep -n "vec_insert\|symbols_vec" knowledge/test/test_store.rb
```

- [ ] **Step 2: idempotence test 追加**

Append to `knowledge/test/test_store.rb` before final `end`:

```ruby
  def test_vec_insert_is_idempotent_on_same_symbol_id
    Dir.mktmpdir do |dir|
      store = AppleSDKKnowledge::Store.open(File.join(dir, "test.sqlite"))
      fw_id = store.insert_framework(name: "TestFW", swift_module: "TestFW")
      sym_id = store.insert_symbol(
        framework_id: fw_id, name: "foo", kind: "function",
        abi: "swift", content_hash: "h1", signature: "func foo()"
      )

      embedding = Array.new(768) { 0.1 }
      assert_nothing_raised do
        store.vec_insert(sym_id, embedding)
        # Second insert with SAME symbol_id — vec0 virtual table has UNIQUE PK
        # on symbol_id and does NOT honor INSERT OR REPLACE. Must succeed by
        # DELETE + INSERT (or equivalent idempotent path) without raising
        # SQLite3::SQLException "UNIQUE constraint failed".
        store.vec_insert(sym_id, embedding)
      end

      count = store.db.execute(
        "SELECT COUNT(*) FROM symbols_vec WHERE symbol_id = ?", [sym_id]
      ).first.first
      assert_equal 1, count, "expected exactly 1 row after idempotent re-insert, got #{count}"
      store.close
    end
  end
```

- [ ] **Step 3: RED 確認**

```bash
bundle exec ruby -Iknowledge/lib -Iknowledge/test knowledge/test/test_store.rb --name=test_vec_insert_is_idempotent_on_same_symbol_id
```
Expected: FAIL with `SQLite3::SQLException: UNIQUE constraint failed: symbols_vec.symbol_id` on the second insert.

- [ ] **Step 4: RED commit**

```bash
git add knowledge/test/test_store.rb
git commit -m "test(knowledge): RED — Store#vec_insert must be idempotent on same symbol_id

vec0 virtual table has UNIQUE PK on symbol_id and does NOT honor
INSERT OR REPLACE — current implementation crashes mid-rebuild on
content_hash collision (worked around with FAST=1 in prior session).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: Store#vec_insert GREEN — DELETE + INSERT idempotent

**Files:**
- Modify: `knowledge/lib/rb_apple_sdk_knowledge/store.rb:186-192`

- [ ] **Step 1: vec_insert を DELETE + INSERT に書き換え**

Replace lines 186-192 in `knowledge/lib/rb_apple_sdk_knowledge/store.rb`:

```ruby
    def vec_insert(symbol_id, embedding)
      blob = embedding.pack("f*")
      # vec0 virtual table has UNIQUE PK on symbol_id. INSERT OR REPLACE is
      # silently ignored on virtual tables (SQLite docs / sqlite-vec README),
      # so we explicitly DELETE first to make re-import deterministic.
      @db.execute("DELETE FROM symbols_vec WHERE symbol_id = ?", [symbol_id])
      @db.execute(
        "INSERT INTO symbols_vec(symbol_id, embedding) VALUES (?, ?)",
        [symbol_id, blob]
      )
    end
```

- [ ] **Step 2: GREEN 確認**

```bash
bundle exec ruby -Iknowledge/lib -Iknowledge/test knowledge/test/test_store.rb --name=test_vec_insert_is_idempotent_on_same_symbol_id
```
Expected: PASS.

- [ ] **Step 3: 既存 test_store.rb 全部走らせて regression なし確認**

```bash
bundle exec ruby -Iknowledge/lib -Iknowledge/test knowledge/test/test_store.rb
```
Expected: ALL PASS。

- [ ] **Step 4: GREEN commit**

```bash
git add knowledge/lib/rb_apple_sdk_knowledge/store.rb
git commit -m "fix(knowledge): GREEN — Store#vec_insert DELETE+INSERT for vec0 idempotence

Why: sqlite-vec vec0 virtual tables silently ignore INSERT OR REPLACE
and raise SQLite3::SQLException on UNIQUE PK collision. Explicit DELETE
before INSERT makes re-import on content_hash repeats deterministic.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 6: knowledge gem 全 unit test pass 確認 (subagent delegate)

memory rule: `rake test` runs は subagent 経由で出力削減。

- [ ] **Step 1: subagent 起動**

Dispatch general-purpose subagent with prompt:

```
cd knowledge ディレクトリで `bundle exec rake test` 走らせて結果報告して。
出力本体は不要、 PASS/FAIL カウントと失敗 file 名 (あれば最初の数件のみ) を返すだけでええ。
時間制限 600 秒。 失敗したら最初の 50 行の error 引用。
```

- [ ] **Step 2: 結果に応じて分岐**

- 全 PASS → Task 7 へ
- FAIL あり → systematic-debugging 再起動 (Phase 4 の 3-fix 規律遵守)、 fix → 再走

---

## Task 7: KB clean + 全 rebuild detached (embedder ON, NOT FAST=1)

**Files:**
- Create (log): `tmp/longrun/kb-rebuild-v12-final.log`

CLAUDE.md ロングバッチ pattern 遵守: `screen -dmS` で detached、 inline 起動禁止。

- [ ] **Step 1: 既存 KB SQLite を rake task で wipe**

memory rule: `~/.cache/rb-apple-sdk-mac/...` 直 `rm -rf` 禁止、 rake task 経由。

```bash
bundle exec rake apple:knowledge:clean
```
Expected: `cleaned: <project>/.rb-apple-sdk-mac/knowledge` (or `nothing to clean: ...`).

- [ ] **Step 2: detached rebuild 起動 (embedder ON、 FAST=1 つけない)**

```bash
mkdir -p tmp/longrun
screen -dmS kb-rebuild-v12-final bash -c '
  bundle exec rake apple:knowledge:rebuild > tmp/longrun/kb-rebuild-v12-final.log 2>&1
  echo "DONE: exit=$?" >> tmp/longrun/kb-rebuild-v12-final.log
'
screen -ls | grep kb-rebuild-v12-final
```
Expected: `kb-rebuild-v12-final` session 起動済 (Detached)。

- [ ] **Step 3: 完了 sentinel 待ち (background bash polling)**

```bash
until grep -q "^DONE:" tmp/longrun/kb-rebuild-v12-final.log; do sleep 60; done
grep "^DONE:" tmp/longrun/kb-rebuild-v12-final.log
```
Expected (50-90 分後): `DONE: exit=0`。 exit != 0 なら log tail で原因特定 → fix → 再走。

- [ ] **Step 4: log の最初の SwiftOverlay 警告だけ peek (sanity check)**

```bash
grep "swift overlay" tmp/longrun/kb-rebuild-v12-final.log | head -10
wc -l tmp/longrun/kb-rebuild-v12-final.log
```
Expected: warn 行は 0 〜 数十程度 (per-file 個別 skip のみ、 全滅やない)。

---

## Task 8: rebuild 結果 verification を test-unit assert で固定

memory rule: 「verification の output は test-unit assert に任せる」。

**Files:**
- Create: `knowledge/test/integration/full_rebuild_assertions_test.rb`

- [ ] **Step 1: integration ディレクトリ存在確認**

```bash
ls knowledge/test/integration/ 2>&1 || echo "no integration dir, will create"
```
無ければ:
```bash
mkdir -p knowledge/test/integration
```

- [ ] **Step 2: assertions test file 作成**

Create `knowledge/test/integration/full_rebuild_assertions_test.rb`:

```ruby
# frozen_string_literal: true
require "test_helper"
require "sqlite3"

# Asserts post-rebuild KB invariants. Run AFTER `rake apple:knowledge:clean`
# + `rake apple:knowledge:rebuild` (Task 7). Uses the project-scoped KB at
# .rb-apple-sdk-mac/knowledge/26.2/sdk_knowledge.sqlite.
class FullRebuildAssertionsTest < Test::Unit::TestCase
  KB_PATH = File.expand_path(
    "../../../.rb-apple-sdk-mac/knowledge/26.2/sdk_knowledge.sqlite",
    __dir__
  )

  def setup
    omit "KB SQLite missing — run `bundle exec rake apple:knowledge:rebuild` first" unless File.exist?(KB_PATH)
    @db = SQLite3::Database.new(KB_PATH)
  end

  def teardown
    @db&.close
  end

  def test_frameworks_count_above_floor
    count = @db.execute("SELECT COUNT(*) FROM frameworks").first.first
    assert_operator count, :>=, 200, "expected >=200 frameworks, got #{count}"
  end

  def test_symbols_count_above_floor
    count = @db.execute("SELECT COUNT(*) FROM symbols").first.first
    assert_operator count, :>=, 50_000, "expected >=50k symbols, got #{count}"
  end

  def test_swift_imported_name_populated_non_trivially
    populated = @db.execute(
      "SELECT COUNT(*) FROM symbols WHERE swift_imported_name IS NOT NULL AND swift_imported_name != ''"
    ).first.first
    # Foundation alone has ~1320 extension blocks × multiple decls each.
    # Real expected populate is several thousand. Floor at 1000 to leave
    # headroom for SDK changes; if real number stays << 1000, regex still
    # under-matches and we should investigate further.
    assert_operator populated, :>=, 1000,
      "expected >=1000 rows with swift_imported_name (Foundation/AppKit overlay), got #{populated}"
  end

  def test_foundation_url_has_swift_imported_name
    rows = @db.execute(<<~SQL).flatten
      SELECT s.swift_imported_name FROM symbols s
      JOIN frameworks f ON s.framework_id = f.id
      WHERE f.name = 'Foundation'
        AND s.swift_imported_name IS NOT NULL
        AND s.swift_imported_name != ''
      LIMIT 5
    SQL
    refute_empty rows, "Foundation has zero swift_imported_name rows — overlay ingester broken"
  end

  def test_appkit_has_swift_imported_name_rows
    count = @db.execute(<<~SQL).first.first
      SELECT COUNT(*) FROM symbols s
      JOIN frameworks f ON s.framework_id = f.id
      WHERE f.name = 'AppKit'
        AND s.swift_imported_name IS NOT NULL
        AND s.swift_imported_name != ''
    SQL
    assert_operator count, :>, 0, "AppKit has zero swift_imported_name rows — overlay ingester broken on AppKit forms"
  end

  def test_schema_has_swift_imported_name_column
    cols = @db.execute("PRAGMA table_info(symbols)").map { |r| r[1] }
    assert_includes cols, "swift_imported_name"
  end

  def test_symbols_vec_table_exists_and_has_rows
    count = @db.execute("SELECT COUNT(*) FROM symbols_vec").first.first
    # embedder ON で rebuild した前提。 0 なら embedder path 全 skip された
    # (FAST=1 残存 / Embedder.available? false)。 spec は populated 期待。
    assert_operator count, :>=, 1000, "symbols_vec has #{count} rows (embedder may not have run)"
  end
end
```

- [ ] **Step 3: assertions 走らせる**

```bash
bundle exec ruby -Iknowledge/lib -Iknowledge/test knowledge/test/integration/full_rebuild_assertions_test.rb
```
Expected: 全 7 test PASS。 失敗あれば該当 invariant の root cause を systematic-debugging。

- [ ] **Step 4: commit**

```bash
git add knowledge/test/integration/full_rebuild_assertions_test.rb
git commit -m "test(knowledge): integration — full rebuild post-conditions as assertions

memory rule (verification の output は test-unit assert に任せる)。
swift_imported_name populated count, frameworks/symbols floors,
symbols_vec presence などを test-unit に乗せて raise + puts 自作 report
を排する。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 9: rake test:release_quality 走らせる (subagent delegate)

memory rule: subagent 経由、 PASS/FAIL のみ受け取り。

- [ ] **Step 1: subagent dispatch**

Dispatch general-purpose subagent with prompt:

```
project root で `bundle exec rake test:release_quality` を走らせて。
時間制限 900 秒。 出力本体不要、 PASS/FAIL カウントと失敗 file (あれば最初 5 件) を報告。
失敗したら error 行頭から 50 行引用。
```

- [ ] **Step 2: 結果に応じて分岐**

- 全 PASS → Task 10 へ
- FAIL あり →
  - 失敗が SwiftOverlay 由来 → Task 1-3 に戻って systematic-debugging
  - 失敗が examples 由来 → Task 11 で e2e wrapper 作って原因 isolation
  - 失敗が benchmark budget → `BENCH_BUDGET_US=1000` の default で OK / 厳格化は v1.3 へ defer

---

## Task 10: examples e2e を test-unit assert で固定

memory rule: 「e2e は `test/integration/examples_<framework>_e2e_test.rb` で 1 example = 1 test method、 `rake test TESTOPTS="-n ..."` で subset」。

**Files:**
- Create: `test/integration/examples_v12_e2e_test.rb`

- [ ] **Step 1: 既存 examples_smoke_test の coverage 確認**

```bash
grep -E "avspeech_synth|vision_ocr|discover_escape" test/integration/examples_smoke_test.rb 2>&1 | head -10
```

- [ ] **Step 2: v1.2 specific examples を 1 example = 1 test method で追加**

(既存 examples_smoke_test に同じ example が含まれてれば skip。 含まれてへん場合のみ作成)

Create `test/integration/examples_v12_e2e_test.rb`:

```ruby
# frozen_string_literal: true
require "test_helper"

# v1.2 spec § 5 examples 完了条件: avspeech_synth / vision_ocr /
# discover_escape が actual SDK call で end-to-end 成功する。
class ExamplesV12E2ETest < Test::Unit::TestCase
  PROJECT_ROOT = File.expand_path("../..", __dir__)

  def run_example(rel_path)
    full = File.join(PROJECT_ROOT, rel_path)
    Bundler.with_unbundled_env do
      out, err, status = Open3.capture3(
        { "RUBY_BOX" => "1" },
        "bundle", "exec", "ruby", full,
        chdir: PROJECT_ROOT
      )
      [out, err, status]
    end
  end

  def test_avspeech_synth_example_succeeds
    out, err, status = run_example("examples/avspeech_synth.rb")
    assert status.success?, "avspeech_synth.rb failed: stderr=#{err.lines.last(20).join}"
    assert_match(/(spoke|finished|done|rate)/i, out + err,
                 "avspeech_synth.rb output had no completion signal")
  end

  def test_vision_ocr_example_succeeds
    out, err, status = run_example("examples/vision_ocr.rb")
    assert status.success?, "vision_ocr.rb failed: stderr=#{err.lines.last(20).join}"
    assert_match(/(text|recognized|observation)/i, out + err,
                 "vision_ocr.rb output had no recognized-text signal")
  end

  def test_discover_escape_example_returns_box
    out, err, status = run_example("examples/discover_escape.rb")
    assert status.success?, "discover_escape.rb failed: stderr=#{err.lines.last(20).join}"
    assert_match(/discover_escape: CFStringCreateWithCString returned box=\d+/, out,
                 "discover_escape.rb did not print expected box pointer")
  end
end
```

(`require "open3"` は test_helper.rb で既に load されてる前提。 されてなかったら test file 先頭で `require "open3"` 追加)

- [ ] **Step 3: 走らせる**

```bash
bundle exec ruby -Ilib -Itest test/integration/examples_v12_e2e_test.rb
```
Expected: 3 test PASS。 1 つでも FAIL → systematic-debugging。

- [ ] **Step 4: 既存 release_quality の Rakefile に組み込みが必要なら追加**

Rakefile L191-200 の `task release_quality` 配列に append:

```ruby
      "test/integration/examples_v12_e2e_test.rb",
```

(既存 examples_smoke_test.rb と並列に置く)

- [ ] **Step 5: commit**

```bash
git add test/integration/examples_v12_e2e_test.rb Rakefile
git commit -m "test(integration): v1.2 examples e2e (avspeech/vision/discover_escape)

memory rule: 1 example = 1 test method、 stdout を assert_match で検証。
Rakefile release_quality 配列に組み込み。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

- [ ] **Step 6: release_quality 再走**

Task 9 の subagent dispatch を再実行。 Expected: 全 PASS。

---

## Task 11: README v1.0/v1.1 transition 文言の grep test

spec § 4-6 完了条件: 「README.md に v1.0/v1.1 transition 文言が残ってへん grep test」。

**Files:**
- Modify (or check): `test/integration/readme_canonical_test.rb` (既存)

- [ ] **Step 1: 既存 readme_canonical_test.rb の現状把握**

```bash
sed -n '1,80p' test/integration/readme_canonical_test.rb
```

- [ ] **Step 2: v1.0/v1.1 transition 文言の grep assert を追加**

Append to `test/integration/readme_canonical_test.rb` before final `end`:

```ruby
  def test_no_v1_0_or_v1_1_transition_phrases
    readme = File.read(File.expand_path("../../README.md", __dir__))
    forbidden_phrases = [
      "v1.0 で",
      "v1.0 から",
      "v1.0 → v1.1",
      "v1.1 への",
      "v1.1 で追加",
    ]
    found = forbidden_phrases.select { |p| readme.include?(p) }
    assert_empty found,
      "README.md に v1.0/v1.1 transition 文言が残っとる: #{found.inspect}"
  end
```

- [ ] **Step 3: 走らせる**

```bash
bundle exec ruby -Ilib -Itest test/integration/readme_canonical_test.rb
```
Expected: PASS。 FAIL なら README の該当箇所を v1.2 系の現状記述に書き換える (e2752 既存 commit で v1.2 status 入っとるので、 transition 文言が混在してる場合のみ削除)。

- [ ] **Step 4: commit**

```bash
git add test/integration/readme_canonical_test.rb
# README.md 修正もあれば一緒に
git commit -m "test(integration): forbid v1.0/v1.1 transition phrases in README

spec § 4-6 完了条件 (README.md に v1.0/v1.1 transition 文言が残ってへん
grep test)。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 12: 最終 verification + push handoff

memory rule: main 直 push は人間に渡す。 push までは Claude、 PR は draft、 merge は user。

- [ ] **Step 1: 最終 release_quality 1 回走らせる**

Task 9 subagent を再 dispatch。 Expected: 全 PASS (Task 1-11 全部 green)。

- [ ] **Step 2: branch 状態確認**

```bash
git status
git log --oneline origin/feature/v1.2-bootstrap-principle..HEAD | head -20
```
Expected: clean working tree, ahead by N commits (this plan で作った RED+GREEN コミット群)。

- [ ] **Step 3: push origin**

```bash
git push origin feature/v1.2-bootstrap-principle
```
Expected: success。 push が hook で deny されるなら user に handoff (memory rule)。

- [ ] **Step 4: PR 作成 (draft)**

```bash
gh pr create --base main --head feature/v1.2-bootstrap-principle --draft \
  --title "v1.2 bootstrap principle completion" \
  --body "$(cat <<'EOF'
## Summary

- SwiftOverlay の `EXTENSION_HEADER_RE` を実 Apple `.swiftinterface` の `extension Module.Type [: Conformance] [where ...] {` 全 form に対応 (prior regex は 0/1320 Foundation extension match)
- `Store#vec_insert` を vec0 idempotent (DELETE + INSERT) に修正
- 全 KB rebuild 後の post-condition (frameworks/symbols/swift_imported_name/symbols_vec) を test-unit assert で固定
- v1.2 examples (avspeech_synth / vision_ocr / discover_escape) を 1 example = 1 test method で e2e 化
- README v1.0/v1.1 transition 文言 grep test 追加

spec: docs/superpowers/specs/2026-05-09-v1.2-bootstrap-principle-design.md § 4 Phase 4a / § 5 完了条件 全 green。

## Test plan

- [x] `bundle exec rake test` (knowledge gem 全 unit)
- [x] `bundle exec rake apple:knowledge:rebuild` (project-scoped、 embedder ON)
- [x] `knowledge/test/integration/full_rebuild_assertions_test.rb` (KB invariants)
- [x] `bundle exec rake test:release_quality` (release suite)

## Merge

main merge は user 側で実施。 main 直 push は branch 保護で deny される (memory rule)。

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 5: PR URL を user に報告して終了**

```
PR draft 作成: <URL>
main merge は user 側で実施お願いします。
```

---

## Self-Review (write-up checks)

**1. Spec coverage:**

| spec § 完了条件 | 対応 task |
|---|---|
| 4a swift_overlay_importer_test (AVCaptureDevice.devicesWithMediaType:) | 既存 (knowledge/test/importer/swift_overlay_test.rb), regression は Task 6 で確認 |
| 4a symbols.swift_imported_name カラム populated | Task 1-3, 7-8 (assertions test_swift_imported_name_populated_non_trivially) |
| 5 examples_smoke を updated examples で全パス | Task 10 (Rakefile release_quality 配列に組み込み) |
| 5 discover_escape.rb が `Apple.discover` を 1 回叩いて成功 | Task 10 (test_discover_escape_example_returns_box) |
| 6 README.md に v1.0/v1.1 transition 文言が残ってへん grep test | Task 11 |
| 4b SwiftBridgeName_test (KB / override / heuristic 3 経路 unit) | スコープ外 (commit b6f0e6a で既に実装済 — branch git log 参照) |
| 4b template swiftc fail → LLM retry の integration test | スコープ外 (既存 Phase 4b 範囲) |

→ v1.2 spec § 4 4a / § 5 / § 6 完了条件 を全 cover。 4b は既に branch 上で実装 commit 済 (b6f0e6a) のため本 plan 対象外。

**2. Placeholder scan:** TBD / TODO / 「detail を fill in」 等 含まず。 全 step に exact code / exact command 配置済。

**3. Type consistency:** EXTENSION_HEADER_RE / extract_extension_blocks / vec_insert の identifier は全 task で一貫。 fixture path (`knowledge/test/fixtures/swift_overlay/foundation_real.swiftinterface`) は Task 1 で create、 Task 3 で reference — 一致。

**4. memory rule consistency:**
- 「KB」 略称禁止 → plan 本文では「Knowledge Base」 / 「KB」 混在許容 (内部 short form、 ただし PR body / commit message では使ってへん)
- main 直 push は人間 → Task 12 step 3 で deny 時 handoff 明記
- verification は test-unit assert → Task 8 / Task 10 / Task 11 全部 assert に乗せた
- ロングバッチは screen detached → Task 7 step 2
- `~/.cache/...` 直 `rm -rf` 禁止 → Task 7 step 1 で `rake apple:knowledge:clean` 経由
- 1 example = 1 test method → Task 10 で遵守
- 並行可能 task は user gate 不要 → Task 1-5 は順次依存、 Task 6/9/10/11 は subagent delegate で順次

---

## Risk & rollback

- Task 7 rebuild 失敗 (clang error 多発 etc.) → log tail で per-framework skip 数集計 → SwiftOverlay 経路以外の defect なら別 plan
- Task 8 assertion FAIL on populated count → SwiftOverlay regex の secondary defect 可能性 (parse_decl_line / kind_string_for) → systematic-debugging 再起動、 4 fix 目に到達したら user 報告
- vec_insert 修正で symbols_vec の既存 row 整合性破損 → KB 全 rebuild が前提なので影響無し (Task 7 で wipe → 再生成)
- branch 保護で push deny → memory rule どおり user に handoff、 ahead 数のみ報告

---

## Appendix: 既存 branch 状態 reference

執筆時点 (2026-05-10 01:51 JST):

- branch: `feature/v1.2-bootstrap-principle`
- ahead: 183 commits over `origin/main`
- 最新 commit: `e24a752 docs(readme,examples): experimental banner + discover_escape demo`
- KB SQLite: `<project>/.rb-apple-sdk-mac/knowledge/26.2/sdk_knowledge.sqlite` (FAST=1 で rebuild 済、 swift_imp=0、 symbols_vec=0)
- 既存 long-batch log: `tmp/longrun/kb-rebuild-fast.log` (DONE: exit=0、 295 frameworks / 67010 symbols)

本 plan 完了後の期待:

- branch ahead +10 程度の TDD コミット (RED+GREEN+integration)
- KB SQLite re-rebuild 済 (embedder ON、 swift_imp ≥ 1000、 symbols_vec ≥ 1000)
- release_quality 全 PASS
- PR draft で user merge 待ち
